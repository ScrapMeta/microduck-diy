---
description: Software — firmware, bus, onboard scripts, system image; ephemeral per Issue
globs:
  - "**/microduck-diy/imu_to_dxl/**"
  - "**/microduck-diy/image/**"
  - "**/microduck-diy/scripts/**"
  - "**/microduck_ros2/**"
alwaysApply: false
---

# Microduck DIY · software

按 Issue 新建的**短命**职能会话，非常驻。

## 领地（可写）

- `microduck-diy/imu_to_dxl/`（固件为主；**勿改网表** → hardware，含其 `scripts/`）
- `microduck-diy/image/`（系统镜像）· `microduck-diy/scripts/`（台架/上机脚本；现无，按需新建）
- ROS2 任务：`microduck_ros2/`（另 `@microduck-ros2` 附件）

## 禁止

- 不改 PCB 原理图/Gerber（→ **hardware**）
- 不跑/改 PPO 训练与域随机（→ **train**）；只消费已导出 ONNX 契约
- 不改 CAD 结构拓扑（→ **structure**）

## 契约

- 机身 IMU：总线从机行为对齐官方 ID 200 / 寄存器块（以 wiki 为准）
- 换执行器或观测维 → 请 **pm** 开/改派 Issue 给 **train**（写清 obs/act）；勿口头改维数

流程见细则 §5 · §6。中文沟通；标识符英文。
